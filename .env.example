# Required — PostgreSQL container only
POSTGRES_DB=interviewprep_db
POSTGRES_USER=interviewprep
POSTGRES_PASSWORD=<your-db-password>

# Required — API container only (rest of secrets come from OCI Vault)
OCI_VAULT_ID=ocid1.vault.oc1...<paste-your-vault-ocid>
